import { Injectable } from '@angular/core';

@Injectable({
	providedIn: 'root',
})
export class BootstrapService {
	async initialize() {
		// Bootstrap initialization logic removed as menu and company features were deleted.
	}
}
